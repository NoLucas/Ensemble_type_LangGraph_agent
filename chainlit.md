# 🔎 GitHub 저장소 리뷰 에이전트

LangGraph 기반 에이전트입니다. 리뷰할 GitHub 저장소를 `owner/repo` 형식으로 알려주세요.

- **요약만**: `octocat/Hello-World 개요만 알려줘`
- **코드 리뷰만**: `langchain-ai/langgraph 소스 코드 리뷰해줘`
- **둘 다**: `langchain-ai/langgraph 요약이랑 코드 리뷰 둘 다 해줘`
- 저장소를 여러 개 언급하면 병렬로 처리됩니다.

도구(저장소 개요 조회, 소스 코드 조회)가 실행되면 채팅 위에 접이식 단계(Step)로 표시됩니다. 최종 리뷰는 동일한 리뷰를 3번 독립적으로 시도한 뒤, 실제 조회 결과와 가장 잘 맞는 답을 자동으로 골라 보여줍니다.

GitHub API는 비인증 시 시간당 60회 제한이 있습니다. 저장소를 자주 리뷰한다면 `.env`에 `GITHUB_TOKEN`을 설정해 시간당 5000회로 늘릴 수 있습니다.
