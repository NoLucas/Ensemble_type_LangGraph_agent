ANTHROPIC_API_KEY=sk-ant-여기에-발급받은-키-붙여넣기

# 선택 사항: 설정하면 GitHub API 호출이 비인증(시간당 60회)에서
# 인증(시간당 5000회)으로 전환된다. https://github.com/settings/tokens
# 에서 별도 권한 없는(공개 저장소 읽기용) 토큰을 발급받아 넣으면 된다.
GITHUB_TOKEN=
